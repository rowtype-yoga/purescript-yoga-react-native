import { StyleSheet } from "react-native";
export const create = (styles) => StyleSheet.create(styles);
export const hairlineWidth = StyleSheet.hairlineWidth;
export const absoluteFill = StyleSheet.absoluteFill;
